# [PATCH] 관리자 기능 4종 수정 가이드

> **수정 범위:** 관리자 패널 관련 백엔드/프런트엔드 4개 항목  
> **수정 파일:** `excel_repository.py`, `routers/admin.py`, `admin_layer.html`, `admin.js`, `master.xlsx 스키마`

---

## PATCH 1 — 컨텐츠 삭제 정상 동작

### 원인

Excel 삭제 시 `content_id`로 행을 찾은 뒤 `DataFrame.drop()`을 호출하지만,  
인덱스 리셋 없이 저장하면 행 번호가 어긋나 실제 삭제가 되지 않거나 엉뚱한 행이 삭제됨.

### `app/repositories/excel_repository.py` — `delete_content` 함수 전체 교체

```python
def delete_content(self, content_id: str) -> bool:
    with _excel_lock:
        df = self._load("contents")
        mask = df["content_id"] == content_id
        if not mask.any():
            return False
        df = df[~mask].reset_index(drop=True)   # ← reset_index 필수
        self._save("contents", df)

        # 연관 확인 기록도 함께 삭제
        df_conf = self._load("confirmations")
        df_conf = df_conf[df_conf["content_id"] != content_id].reset_index(drop=True)
        self._save("confirmations", df_conf)

        return True
```

### `app/routers/admin.py` — DELETE 엔드포인트 응답 코드 수정

```python
@router.delete("/api/admin/contents/{content_id}")
async def delete_content(
    content_id: str,
    _=Depends(require_admin_user)
):
    success = content_service.delete_content(content_id)
    if not success:
        raise HTTPException(status_code=404, detail="존재하지 않는 컨텐츠입니다.")
    return JSONResponse(status_code=200, content={"status": "deleted", "content_id": content_id})
```

### `static/js/admin.js` — 삭제 버튼 핸들러 수정

```javascript
async function deleteContent(contentId) {
  if (!confirm("삭제하면 복구할 수 없습니다. 계속하시겠습니까?")) return;

  const res = await fetch(`/api/admin/contents/${contentId}`, {
    method: "DELETE",
    credentials: "include",
  });

  if (res.ok) {
    // 목록에서 해당 행 즉시 제거 (페이지 재로드 없이)
    document.querySelector(`[data-content-id="${contentId}"]`)?.closest("tr").remove();
  } else {
    const err = await res.json();
    alert(`삭제 실패: ${err.detail}`);
  }
}
```

---

## PATCH 2 — 새 컨텐츠 작성 시 대상자를 DB 명단에서 선택

### `app/routers/admin.py` — 사용자 목록 반환 엔드포인트 추가

```python
@router.get("/api/admin/users")
async def list_users(_=Depends(require_admin_user)):
    users = user_repo.list_all_users()
    return [
        {"emp_id": u.emp_id, "name": u.name}
        for u in users if u.is_active
    ]
```

### `app/templates/fragments/admin_layer.html` — 대상자 선택 `<select>` 추가

기존 `<input type="text" name="target_emp_id">` 부분을 아래로 교체한다.

```html
<!-- 기존 input 태그 삭제 후 아래로 교체 -->
<div class="form-group">
  <label for="target-emp-select">대상자</label>
  <select id="target-emp-select" required>
    <option value="">-- 대상자 선택 --</option>
    <!-- JS에서 동적 주입 -->
  </select>
</div>
```

### `static/js/admin.js` — 폼 열릴 때 사용자 목록 fetch 후 `<select>` 채우기

```javascript
async function loadUserOptions() {
  const res = await fetch("/api/admin/users", { credentials: "include" });
  const users = await res.json();

  const select = document.getElementById("target-emp-select");
  select.innerHTML = `<option value="">-- 대상자 선택 --</option>`;

  users.forEach(({ emp_id, name }) => {
    const opt = document.createElement("option");
    opt.value = emp_id;
    opt.textContent = `${name} (${emp_id})`;
    select.appendChild(opt);
  });
}

// 컨텐츠 작성 폼을 여는 함수 내부에서 호출
async function openCreateForm() {
  await loadUserOptions();
  document.getElementById("create-form-modal").classList.remove("hidden");
}
```

### `app/routers/admin.py` — 컨텐츠 생성 엔드포인트 수신 모델 확인

```python
class ContentCreate(BaseModel):
    title: str
    target_emp_id: str          # select에서 선택된 사번 수신
    body_1: str
    body_2: str | None = None
    body_3: str | None = None
    response_type: str = "checkbox"   # PATCH 4 항목 추가 (체크박스 or 입력)

@router.post("/api/admin/contents")
async def create_content(
    payload: ContentCreate,
    current_user=Depends(require_admin_user)
):
    content = content_service.create_content(payload, creator_id=current_user["sub"])
    return content
```

---

## PATCH 3 — 확인 현황: 컨텐츠 ID + 대상 사번 필터 조회

### `master.xlsx` — `confirmations` 시트 스키마 변경 없음 (기존 유지)

기존 컬럼(`content_id`, `emp_id`, `is_confirmed`, `confirmed_at`)을 그대로 활용한다.

### `app/repositories/excel_repository.py` — 필터 조회 메서드 추가

```python
def get_confirmations_filtered(
    self,
    content_id: str | None = None,
    emp_id: str | None = None
) -> list[dict]:
    df_conf = self._load("confirmations")
    df_cont = self._load("contents")
    df_user = self._load("users")

    # 컨텐츠 제목, 사용자 이름 조인
    merged = (
        df_conf
        .merge(df_cont[["content_id", "title"]], on="content_id", how="left")
        .merge(df_user[["emp_id", "name"]], on="emp_id", how="left")
    )

    if content_id:
        merged = merged[merged["content_id"] == content_id]
    if emp_id:
        merged = merged[merged["emp_id"] == emp_id]

    return merged[[
        "content_id", "title", "emp_id", "name",
        "is_confirmed", "confirmed_at"
    ]].to_dict(orient="records")
```

### `app/routers/admin.py` — 확인 현황 엔드포인트 추가

```python
@router.get("/api/admin/confirmations")
async def get_confirmations(
    content_id: str | None = None,   # 쿼리 파라미터 (선택)
    emp_id:     str | None = None,   # 쿼리 파라미터 (선택)
    _=Depends(require_admin_user)
):
    results = content_repo.get_confirmations_filtered(content_id, emp_id)
    return results
```

### `app/templates/fragments/admin_layer.html` — 확인 현황 필터 UI 추가

```html
<section id="confirmation-panel">
  <h3>확인 현황 조회</h3>

  <div class="filter-row">
    <!-- 컨텐츠 선택 -->
    <select id="filter-content-id">
      <option value="">전체 컨텐츠</option>
      <!-- JS에서 동적 주입 -->
    </select>

    <!-- 대상 사번 선택 -->
    <select id="filter-emp-id">
      <option value="">전체 대상자</option>
      <!-- JS에서 동적 주입 -->
    </select>

    <button onclick="loadConfirmations()">조회</button>
  </div>

  <table id="confirmation-table">
    <thead>
      <tr>
        <th>컨텐츠 ID</th><th>제목</th>
        <th>사번</th><th>이름</th>
        <th>확인 여부</th><th>확인 일시</th>
      </tr>
    </thead>
    <tbody id="confirmation-tbody"></tbody>
  </table>
</section>
```

### `static/js/admin.js` — 필터 조회 함수 추가

```javascript
async function loadConfirmations() {
  const contentId = document.getElementById("filter-content-id").value;
  const empId     = document.getElementById("filter-emp-id").value;

  const params = new URLSearchParams();
  if (contentId) params.append("content_id", contentId);
  if (empId)     params.append("emp_id", empId);

  const res = await fetch(`/api/admin/confirmations?${params}`, {
    credentials: "include"
  });
  const rows = await res.json();

  const tbody = document.getElementById("confirmation-tbody");
  tbody.innerHTML = rows.map(r => `
    <tr>
      <td>${r.content_id}</td>
      <td>${r.title ?? "-"}</td>
      <td>${r.emp_id}</td>
      <td>${r.name ?? "-"}</td>
      <td>${r.is_confirmed === "TRUE" || r.is_confirmed === true ? "✅ 확인" : "⬜ 미확인"}</td>
      <td>${r.confirmed_at ?? "-"}</td>
    </tr>
  `).join("");
}

// 패널 열릴 때 필터용 선택지 채우기
async function initConfirmationPanel() {
  // 컨텐츠 목록
  const cRes = await fetch("/api/admin/contents", { credentials: "include" });
  const contents = await cRes.json();
  const cSel = document.getElementById("filter-content-id");
  contents.forEach(c => {
    const opt = document.createElement("option");
    opt.value = c.content_id;
    opt.textContent = `${c.title} (${c.content_id.slice(0, 8)}…)`;
    cSel.appendChild(opt);
  });

  // 사용자 목록
  const uRes = await fetch("/api/admin/users", { credentials: "include" });
  const users = await uRes.json();
  const uSel = document.getElementById("filter-emp-id");
  users.forEach(u => {
    const opt = document.createElement("option");
    opt.value = u.emp_id;
    opt.textContent = `${u.name} (${u.emp_id})`;
    uSel.appendChild(opt);
  });

  await loadConfirmations();  // 초기 전체 조회
}
```

---

## PATCH 4 — 컨텐츠별 응답 방식 선택 (체크박스 or 텍스트 입력)

### `master.xlsx` — `contents` 시트 컬럼 1개 추가

| 추가 컬럼 | 타입 | 허용값 | 설명 |
|---|---|---|---|
| `response_type` | str | `checkbox` \| `text` | 사용자 응답 방식 |

기존 Excel 파일의 `contents` 시트에 `response_type` 컬럼을 추가하고,  
기존 행은 모두 `checkbox`로 채운다.

### `app/models/content.py` — Pydantic 모델 필드 추가

```python
from typing import Literal

class ContentCreate(BaseModel):
    title: str
    target_emp_id: str
    body_1: str
    body_2: str | None = None
    body_3: str | None = None
    response_type: Literal["checkbox", "text"] = "checkbox"   # ← 추가

class Content(ContentCreate):
    content_id: str
    created_by: str
    created_at: str
    updated_at: str | None = None
```

### `app/repositories/excel_repository.py` — `create_content` 저장 시 컬럼 포함

```python
def create_content(self, payload, creator_id: str) -> dict:
    with _excel_lock:
        df = self._load("contents")
        new_row = {
            "content_id":    str(uuid.uuid4()),
            "title":         payload.title,
            "target_emp_id": payload.target_emp_id,
            "body_1":        payload.body_1,
            "body_2":        payload.body_2 or "",
            "body_3":        payload.body_3 or "",
            "response_type": payload.response_type,   # ← 추가
            "created_by":    creator_id,
            "created_at":    datetime.now().isoformat(),
            "updated_at":    "",
        }
        df = pd.concat([df, pd.DataFrame([new_row])], ignore_index=True)
        self._save("contents", df)
        return new_row
```

### `app/templates/fragments/admin_layer.html` — 작성 폼에 응답 방식 선택 추가

```html
<div class="form-group">
  <label>응답 방식</label>
  <label>
    <input type="radio" name="response_type" value="checkbox" checked>
    체크박스 (열람 확인)
  </label>
  <label>
    <input type="radio" name="response_type" value="text">
    텍스트 입력 (직접 입력)
  </label>
</div>
```

### `app/templates/fragments/content_layer.html` — `response_type`에 따라 UI 분기

```html
{% for content in contents %}
<div class="content-card" data-content-id="{{ content.content_id }}">
  <h4>{{ content.title }}</h4>
  <p>{{ content.body_1 }}</p>
  {% if content.body_2 %}<p>{{ content.body_2 }}</p>{% endif %}
  {% if content.body_3 %}<p>{{ content.body_3 }}</p>{% endif %}

  <!-- 응답 방식 분기 -->
  {% if content.response_type == "checkbox" %}
    <label class="confirm-label">
      <input
        type="checkbox"
        class="confirm-checkbox"
        data-content-id="{{ content.content_id }}"
        {% if content.is_confirmed %}checked{% endif %}
        onchange="handleConfirmToggle('{{ content.content_id }}', this)"
      >
      확인했습니다
    </label>

  {% elif content.response_type == "text" %}
    <div class="text-response-wrap">
      <textarea
        id="text-{{ content.content_id }}"
        class="confirm-textarea"
        placeholder="내용을 입력하세요"
        {% if content.is_confirmed %}readonly{% endif %}
      >{{ content.confirmed_text or "" }}</textarea>
      {% if not content.is_confirmed %}
        <button onclick="handleTextSubmit('{{ content.content_id }}')">제출</button>
      {% else %}
        <span class="confirmed-badge">✅ 제출 완료</span>
      {% endif %}
    </div>
  {% endif %}

</div>
{% endfor %}
```

### `static/js/content.js` — 텍스트 제출 핸들러 추가

```javascript
// 기존 체크박스 핸들러 (변경 없음)
async function handleConfirmToggle(contentId, checkbox) {
  const confirmed = checkbox.checked;
  const res = await fetch(`/api/content/${contentId}/confirm`, {
    method: "PATCH",
    credentials: "include",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ confirmed, response_type: "checkbox", text: "" }),
  });
  if (!res.ok) {
    checkbox.checked = !confirmed;
    alert("저장에 실패했습니다.");
  }
}

// 신규: 텍스트 제출 핸들러
async function handleTextSubmit(contentId) {
  const textarea = document.getElementById(`text-${contentId}`);
  const text = textarea.value.trim();
  if (!text) { alert("내용을 입력해주세요."); return; }

  const res = await fetch(`/api/content/${contentId}/confirm`, {
    method: "PATCH",
    credentials: "include",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ confirmed: true, response_type: "text", text }),
  });

  if (res.ok) {
    textarea.readOnly = true;
    textarea.nextElementSibling?.remove();   // 제출 버튼 제거
    const badge = document.createElement("span");
    badge.className = "confirmed-badge";
    badge.textContent = "✅ 제출 완료";
    textarea.insertAdjacentElement("afterend", badge);
  } else {
    alert("제출에 실패했습니다.");
  }
}
```

### `app/routers/content.py` — confirm 엔드포인트 수신 모델 확장

```python
class ConfirmPayload(BaseModel):
    confirmed: bool
    response_type: Literal["checkbox", "text"] = "checkbox"
    text: str = ""          # response_type=text 일 때 입력값

@router.patch("/api/content/{content_id}/confirm")
async def confirm_content(
    content_id: str,
    payload: ConfirmPayload,
    current_user=Depends(require_authenticated_user)
):
    success = content_service.set_confirmation(
        content_id=content_id,
        emp_id=current_user["sub"],
        confirmed=payload.confirmed,
        confirmed_text=payload.text,   # ← 추가
    )
    if not success:
        raise HTTPException(status_code=404, detail="컨텐츠를 찾을 수 없습니다.")
    return {"status": "ok"}
```

### `app/repositories/excel_repository.py` — `set_confirmation` 텍스트 저장 추가

```python
def set_confirmation(
    self,
    content_id: str,
    emp_id: str,
    confirmed: bool,
    confirmed_text: str = ""
) -> bool:
    with _excel_lock:
        df = self._load("confirmations")
        mask = (df["content_id"] == content_id) & (df["emp_id"] == emp_id)

        if mask.any():
            df.loc[mask, "is_confirmed"]   = str(confirmed).upper()
            df.loc[mask, "confirmed_at"]   = datetime.now().isoformat() if confirmed else ""
            df.loc[mask, "confirmed_text"] = confirmed_text   # ← 추가 컬럼
        else:
            new_row = {
                "confirmation_id": str(uuid.uuid4()),
                "content_id":      content_id,
                "emp_id":          emp_id,
                "is_confirmed":    str(confirmed).upper(),
                "confirmed_at":    datetime.now().isoformat() if confirmed else "",
                "confirmed_text":  confirmed_text,            # ← 추가 컬럼
            }
            df = pd.concat([df, pd.DataFrame([new_row])], ignore_index=True)

        self._save("confirmations", df)
        return True
```

### `master.xlsx` — `confirmations` 시트 컬럼 1개 추가

| 추가 컬럼 | 타입 | 설명 |
|---|---|---|
| `confirmed_text` | str | `response_type=text` 일 때 사용자가 입력한 텍스트. 체크박스 방식은 빈 문자열. |

---

## 수정 요약

| PATCH | 수정 파일 | 핵심 변경 |
|---|---|---|
| 1. 삭제 버그 | `excel_repository.py`, `routers/admin.py`, `admin.js` | `reset_index(drop=True)` + 연관 확인 기록 동시 삭제 |
| 2. 대상자 선택 | `routers/admin.py`, `admin_layer.html`, `admin.js` | `/api/admin/users` 목록 fetch 후 `<select>` 동적 주입 |
| 3. 확인 현황 필터 | `excel_repository.py`, `routers/admin.py`, `admin_layer.html`, `admin.js` | `content_id` + `emp_id` 쿼리 파라미터 필터 조회 |
| 4. 응답 방식 분기 | `contents` 스키마, `models/content.py`, `content_layer.html`, `content.js`, `routers/content.py`, `excel_repository.py` | `response_type` 컬럼 추가 + 체크박스/텍스트 분기 렌더링 |
