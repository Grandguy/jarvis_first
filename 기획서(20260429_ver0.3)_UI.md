# [PATCH] UI 레이아웃 확장 및 컨텐츠 작성 폼 개편

> **수정 범위:** 전체 페이지 레이아웃, 컨텐츠 작성 모달, 데이터 스키마  
> **수정 파일:** `main.css`, `admin_layer.html`, `admin.js`, `content_layer.html`, `content.js`, `models/content.py`, `excel_repository.py`, `routers/admin.py`  
> **이전 PATCH 4 충돌 주의:** `response_type(checkbox|text)` 컬럼을 `answer_type(yes_no|text)`으로 대체함

---

## PATCH A — 전체 페이지 레이아웃: 13인치 노트북 기준 확장

> 기준 해상도: **1280 × 800** (13인치 노트북 평균)  
> 콘텐츠 최대 너비를 뷰포트에 맞게 확장하고, 불필요한 여백을 제거한다.

### `static/css/main.css` — 레이아웃 전역 수정

```css
/* ── 기존 코드에서 아래 항목을 찾아 값만 교체한다 ── */

/* 1. 전체 최대 너비 확장 */
/* 변경 전: max-width: 960px 또는 유사한 값 */
/* 변경 후: */
.app-container,
#app-root,
.content-wrap {
  max-width: 1240px;          /* 1280px - 좌우 패딩 40px */
  width: 100%;
  margin: 0 auto;
  padding: 0 20px;
  box-sizing: border-box;
}

/* 2. 바디 기본 여백 초기화 */
*, *::before, *::after { box-sizing: border-box; }

body {
  margin: 0;
  padding: 0;
  font-family: 'Pretendard', 'Apple SD Gothic Neo', sans-serif;
  font-size: 14px;
  background: #f5f6fa;
  color: #1a1a2e;
  min-width: 1280px;          /* 1280 미만에서 가로 스크롤 허용 */
}

/* 3. 사이드바 + 메인 2단 레이아웃 (관리자 패널) */
.admin-layout {
  display: grid;
  grid-template-columns: 220px 1fr;
  grid-template-rows: 56px 1fr;
  height: 100vh;
  gap: 0;
}

.admin-sidebar {
  grid-row: 1 / 3;
  width: 220px;
  background: #1a1a2e;
  padding: 24px 0;
}

.admin-header {
  grid-column: 2;
  height: 56px;
  background: #fff;
  border-bottom: 1px solid #e0e0e0;
  display: flex;
  align-items: center;
  padding: 0 32px;
}

.admin-main {
  grid-column: 2;
  padding: 32px;
  overflow-y: auto;
}

/* 4. 사용자 컨텐츠 영역 (단일 컬럼, 좌우 여백 확보) */
.user-layout {
  max-width: 1240px;
  margin: 0 auto;
  padding: 40px 20px;
}

/* 5. 테이블 전역 스타일 — 넓게 사용 */
.data-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 13px;
  background: #fff;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 1px 4px rgba(0,0,0,.08);
}

.data-table th {
  background: #f0f2f8;
  padding: 12px 16px;
  text-align: left;
  font-weight: 600;
  color: #444;
  white-space: nowrap;
}

.data-table td {
  padding: 11px 16px;
  border-bottom: 1px solid #f0f0f0;
  vertical-align: middle;
}

.data-table tr:last-child td { border-bottom: none; }
.data-table tr:hover td { background: #fafbff; }

/* 6. 모달 — 넓게 */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,.45);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.modal-box {
  background: #fff;
  border-radius: 12px;
  width: min(1100px, 90vw);   /* 최대 1100px, 뷰포트 90% */
  max-height: 88vh;
  overflow-y: auto;
  padding: 40px 48px;
  box-shadow: 0 8px 40px rgba(0,0,0,.18);
}

/* 7. 숨김 유틸리티 */
.hidden { display: none !important; }
```

---

## PATCH B — 컨텐츠 작성 모달: 테이블 기반 와이드 레이아웃

### 전체 구조 변경 방향

```
┌─────────────────────────────────────── modal-box (1100px) ───────────────┐
│ 제목 ──────────────────────────────────────────────────────────── [닫기] │
├──────────────────────────────────────────────────────────────────────────┤
│  ┌─── 좌 패널 (480px) ────────────────┐  ┌─── 우 패널 (flex:1) ────────┐ │
│  │  [대상자 선택]                      │  │  제목 입력                  │ │
│  │  ┌ 검색 + 전체선택 헤더 ─────────┐ │  │  내용 입력                  │ │
│  │  │ ☑ 전체선택   [ 검색창 ]       │ │  │  답변 유형 선택             │ │
│  │  ├──────────────────────────────┤ │  │                             │ │
│  │  │ ☐ 홍길동 (EMP001)            │ │  │                             │ │
│  │  │ ☑ 김철수 (EMP002)            │ │  │                             │ │
│  │  │ ...                          │ │  │                             │ │
│  │  └──────────────────────────────┘ │  └─────────────────────────────┘ │
│  │  선택됨: 2명                       │                                  │
│  └────────────────────────────────────┘                                  │
│                                              [취소]  [컨텐츠 생성]       │
└──────────────────────────────────────────────────────────────────────────┘
```

### `app/templates/fragments/admin_layer.html` — 새 컨텐츠 모달 전체 교체

기존 `id="create-content-modal"` 블록 전체를 아래로 교체한다.

```html
<!-- 새 컨텐츠 작성 모달 -->
<div id="create-content-modal" class="modal-overlay hidden">
  <div class="modal-box">

    <!-- 헤더 -->
    <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:28px;">
      <h2 style="margin:0; font-size:18px;">새 컨텐츠 작성</h2>
      <button onclick="closeCreateModal()"
              style="background:none; border:none; font-size:22px; cursor:pointer; color:#888;">✕</button>
    </div>

    <!-- 2단 레이아웃 -->
    <div style="display:grid; grid-template-columns:480px 1fr; gap:40px; align-items:start;">

      <!-- ── 좌: 대상자 선택 패널 ── -->
      <div>
        <p style="font-weight:600; margin:0 0 10px;">대상자 선택</p>

        <!-- 전체선택 + 검색 헤더 -->
        <div style="display:flex; align-items:center; gap:12px;
                    background:#f5f6fa; border:1px solid #dde1ee;
                    border-bottom:none; border-radius:8px 8px 0 0;
                    padding:10px 14px;">
          <label style="display:flex; align-items:center; gap:6px; cursor:pointer; white-space:nowrap;">
            <input type="checkbox" id="select-all-users" onchange="toggleSelectAll(this)">
            전체선택
          </label>
          <input type="text" id="user-search" placeholder="이름 또는 사번 검색"
                 oninput="filterUserList(this.value)"
                 style="flex:1; padding:6px 10px; border:1px solid #ccc;
                        border-radius:4px; font-size:13px;">
        </div>

        <!-- 사용자 목록 테이블 -->
        <div style="border:1px solid #dde1ee; border-radius:0 0 8px 8px;
                    max-height:340px; overflow-y:auto;">
          <table class="data-table" style="box-shadow:none; border-radius:0;">
            <thead style="position:sticky; top:0; z-index:1;">
              <tr>
                <th style="width:36px;"></th>
                <th>이름</th>
                <th>사번</th>
                <th>이메일</th>
              </tr>
            </thead>
            <tbody id="user-select-tbody">
              <!-- JS에서 동적 주입 -->
            </tbody>
          </table>
        </div>
        <p id="selected-count" style="font-size:12px; color:#666; margin:8px 0 0;">선택됨: 0명</p>
      </div>

      <!-- ── 우: 컨텐츠 입력 패널 ── -->
      <div style="display:flex; flex-direction:column; gap:20px;">

        <!-- 제목 -->
        <div>
          <label style="font-weight:600; display:block; margin-bottom:6px;">제목</label>
          <input type="text" id="content-title" placeholder="컨텐츠 제목"
                 style="width:100%; padding:10px 12px; border:1px solid #dde1ee;
                        border-radius:6px; font-size:14px;">
        </div>

        <!-- 내용 -->
        <div>
          <label style="font-weight:600; display:block; margin-bottom:6px;">내용</label>
          <textarea id="content-body1" rows="6" placeholder="내용을 입력하세요"
                    style="width:100%; padding:10px 12px; border:1px solid #dde1ee;
                           border-radius:6px; font-size:14px; resize:vertical;"></textarea>
        </div>

        <!-- 답변 유형 선택 (Yes/No ↔ 문구 입력) -->
        <div>
          <label style="font-weight:600; display:block; margin-bottom:10px;">답변 유형</label>
          <div style="display:flex; gap:16px;">

            <label style="display:flex; align-items:center; gap:8px; cursor:pointer;
                          border:1px solid #dde1ee; border-radius:8px; padding:14px 20px;
                          flex:1; transition:border-color .2s;"
                   id="label-yes-no">
              <input type="radio" name="answer_type" value="yes_no" checked
                     onchange="updateAnswerTypeUI()">
              <div>
                <div style="font-weight:600; font-size:14px;">Yes / No</div>
                <div style="font-size:12px; color:#888; margin-top:2px;">버튼으로 동의 여부 응답</div>
              </div>
            </label>

            <label style="display:flex; align-items:center; gap:8px; cursor:pointer;
                          border:1px solid #dde1ee; border-radius:8px; padding:14px 20px;
                          flex:1; transition:border-color .2s;"
                   id="label-text">
              <input type="radio" name="answer_type" value="text"
                     onchange="updateAnswerTypeUI()">
              <div>
                <div style="font-weight:600; font-size:14px;">문구 입력</div>
                <div style="font-size:12px; color:#888; margin-top:2px;">직접 텍스트를 입력하여 응답</div>
              </div>
            </label>

          </div>
        </div>

        <!-- 버튼 -->
        <div style="display:flex; justify-content:flex-end; gap:12px; margin-top:8px;">
          <button onclick="closeCreateModal()"
                  style="padding:10px 24px; border:1px solid #ccc; border-radius:6px;
                         background:#fff; cursor:pointer; font-size:14px;">취소</button>
          <button onclick="submitCreateContent()"
                  style="padding:10px 28px; border:none; border-radius:6px;
                         background:#3b5bdb; color:#fff; cursor:pointer;
                         font-size:14px; font-weight:600;">컨텐츠 생성</button>
        </div>

      </div>
    </div>
  </div>
</div>
```

---

## PATCH C — `admin.js`: 모달 제어 및 다중 선택 로직

기존 `admin.js`에서 `openCreateForm`, `submitCreateContent` 함수를 아래로 교체한다.

```javascript
// ── 모달 열기/닫기 ──
async function openCreateModal() {
  await loadUserSelectList();
  updateAnswerTypeUI();
  updateSelectedCount();
  document.getElementById("create-content-modal").classList.remove("hidden");
}

function closeCreateModal() {
  document.getElementById("create-content-modal").classList.add("hidden");
  // 입력 초기화
  document.getElementById("content-title").value = "";
  document.getElementById("content-body1").value = "";
  document.getElementById("user-search").value = "";
  document.querySelector('input[name="answer_type"][value="yes_no"]').checked = true;
  updateAnswerTypeUI();
}

// ── 사용자 목록 로드 & 테이블 렌더 ──
let _allUsers = [];

async function loadUserSelectList() {
  const res = await fetch("/api/admin/users", { credentials: "include" });
  _allUsers = await res.json();
  renderUserTable(_allUsers);
}

function renderUserTable(users) {
  const tbody = document.getElementById("user-select-tbody");
  tbody.innerHTML = users.map(u => `
    <tr data-emp-id="${u.emp_id}" data-name="${u.name}">
      <td style="text-align:center;">
        <input type="checkbox" class="user-checkbox" value="${u.emp_id}"
               onchange="updateSelectedCount()">
      </td>
      <td>${u.name}</td>
      <td>${u.emp_id}</td>
      <td style="font-size:12px; color:#888;">${u.email ?? ""}</td>
    </tr>
  `).join("");
}

// ── 전체선택 토글 ──
function toggleSelectAll(master) {
  document.querySelectorAll(".user-checkbox").forEach(cb => {
    cb.checked = master.checked;
  });
  updateSelectedCount();
}

// ── 실시간 검색 필터 ──
function filterUserList(query) {
  const q = query.toLowerCase();
  document.querySelectorAll("#user-select-tbody tr").forEach(tr => {
    const name = tr.dataset.name?.toLowerCase() ?? "";
    const id   = tr.dataset.empId?.toLowerCase() ?? "";
    tr.style.display = (name.includes(q) || id.includes(q)) ? "" : "none";
  });
}

// ── 선택 카운트 업데이트 ──
function updateSelectedCount() {
  const count = document.querySelectorAll(".user-checkbox:checked").length;
  document.getElementById("selected-count").textContent = `선택됨: ${count}명`;

  const total = document.querySelectorAll(".user-checkbox").length;
  document.getElementById("select-all-users").checked = count === total && total > 0;
  document.getElementById("select-all-users").indeterminate = count > 0 && count < total;
}

// ── 답변 유형 UI 강조 ──
function updateAnswerTypeUI() {
  const val = document.querySelector('input[name="answer_type"]:checked')?.value;
  document.getElementById("label-yes-no").style.borderColor = val === "yes_no" ? "#3b5bdb" : "#dde1ee";
  document.getElementById("label-text").style.borderColor   = val === "text"   ? "#3b5bdb" : "#dde1ee";
}

// ── 컨텐츠 생성 제출 ──
async function submitCreateContent() {
  const title  = document.getElementById("content-title").value.trim();
  const body1  = document.getElementById("content-body1").value.trim();
  const answer = document.querySelector('input[name="answer_type"]:checked')?.value;
  const targets = [...document.querySelectorAll(".user-checkbox:checked")].map(cb => cb.value);

  if (!title)             { alert("제목을 입력하세요."); return; }
  if (!body1)             { alert("내용을 입력하세요."); return; }
  if (targets.length === 0) { alert("대상자를 1명 이상 선택하세요."); return; }

  // 대상자가 여러 명이면 각각 개별 컨텐츠 생성
  const results = await Promise.all(targets.map(emp_id =>
    fetch("/api/admin/contents", {
      method: "POST",
      credentials: "include",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        title, body_1: body1,
        target_emp_id: emp_id,
        answer_type: answer,   // "yes_no" | "text"
      }),
    })
  ));

  const failed = results.filter(r => !r.ok).length;
  if (failed > 0) {
    alert(`${targets.length}명 중 ${failed}명 생성 실패. 나머지는 정상 처리되었습니다.`);
  } else {
    alert(`${targets.length}명에게 컨텐츠가 생성되었습니다.`);
  }

  closeCreateModal();
  await refreshContentTable();   // 컨텐츠 목록 갱신 (기존 함수 유지)
}
```

---

## PATCH D — 스키마: `response_type` → `answer_type` 교체

> 이전 PATCH 4에서 추가한 `response_type(checkbox|text)` 컬럼을 삭제하고  
> `answer_type(yes_no|text)`으로 대체한다.

### `master.xlsx` — `contents` 시트 컬럼 변경

| 삭제 컬럼 | 대체 컬럼 | 타입 | 허용값 |
|---|---|---|---|
| `response_type` | `answer_type` | str | `yes_no` \| `text` |

기존 행의 `answer_type`은 모두 `yes_no`로 초기값 채움.

### `app/models/content.py`

```python
from typing import Literal

class ContentCreate(BaseModel):
    title: str
    target_emp_id: str
    body_1: str
    body_2: str | None = None
    body_3: str | None = None
    answer_type: Literal["yes_no", "text"] = "yes_no"  # response_type 대체
```

### `app/repositories/excel_repository.py` — `create_content`

```python
new_row = {
    ...
    "answer_type": payload.answer_type,   # "response_type" → "answer_type" 으로 키 교체
    ...
}
```

---

## PATCH E — 사용자 컨텐츠 카드: 답변 유형 분기

### `app/templates/fragments/content_layer.html`

```html
{% for content in contents %}
<div class="content-card"
     style="background:#fff; border-radius:10px; padding:28px 32px;
            margin-bottom:20px; box-shadow:0 1px 4px rgba(0,0,0,.08);">

  <h4 style="margin:0 0 12px; font-size:16px;">{{ content.title }}</h4>
  <p  style="margin:0 0 20px; color:#444; line-height:1.7;">{{ content.body_1 }}</p>

  <!-- Yes/No 답변 유형 -->
  {% if content.answer_type == "yes_no" %}
    {% if content.is_confirmed %}
      <span style="display:inline-block; padding:8px 20px; border-radius:6px;
                   background:#e8f5e9; color:#2e7d32; font-weight:600;">
        ✅ {{ content.confirmed_text }}
      </span>
    {% else %}
      <div style="display:flex; gap:12px;">
        <button onclick="handleYesNo('{{ content.content_id }}', 'Yes')"
                style="padding:10px 32px; border-radius:6px; border:none;
                       background:#3b5bdb; color:#fff; font-size:14px;
                       font-weight:600; cursor:pointer;">Yes</button>
        <button onclick="handleYesNo('{{ content.content_id }}', 'No')"
                style="padding:10px 32px; border-radius:6px;
                       border:1px solid #ccc; background:#fff;
                       font-size:14px; cursor:pointer;">No</button>
      </div>
    {% endif %}

  <!-- 문구 입력 답변 유형 -->
  {% elif content.answer_type == "text" %}
    {% if content.is_confirmed %}
      <div style="background:#f5f6fa; border-radius:6px; padding:12px 16px;
                  color:#333; font-size:14px;">
        {{ content.confirmed_text }}
        <span style="margin-left:12px; color:#2e7d32; font-weight:600;">✅ 제출 완료</span>
      </div>
    {% else %}
      <textarea id="text-{{ content.content_id }}" rows="3"
                placeholder="내용을 입력하세요"
                style="width:100%; padding:10px; border:1px solid #dde1ee;
                       border-radius:6px; font-size:14px; resize:vertical;
                       margin-bottom:8px;"></textarea>
      <button onclick="handleTextSubmit('{{ content.content_id }}')"
              style="padding:9px 24px; border:none; border-radius:6px;
                     background:#3b5bdb; color:#fff; cursor:pointer;
                     font-size:14px; font-weight:600;">제출</button>
    {% endif %}
  {% endif %}

</div>
{% endfor %}
```

### `static/js/content.js` — Yes/No 핸들러 추가 및 텍스트 핸들러 유지

```javascript
// Yes / No 응답
async function handleYesNo(contentId, answer) {
  const res = await fetch(`/api/content/${contentId}/confirm`, {
    method: "PATCH",
    credentials: "include",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ confirmed: true, answer_type: "yes_no", text: answer }),
  });
  if (res.ok) {
    // 버튼 영역을 완료 뱃지로 교체
    const card = document.querySelector(`[data-content-id="${contentId}"]`);
    const btnWrap = card?.querySelector(".yesno-wrap");
    if (btnWrap) {
      btnWrap.outerHTML = `<span style="display:inline-block;padding:8px 20px;
        border-radius:6px;background:#e8f5e9;color:#2e7d32;font-weight:600;">
        ✅ ${answer}</span>`;
    }
  } else {
    alert("저장에 실패했습니다.");
  }
}

// 문구 입력 응답 (기존 유지, answer_type 키만 교체)
async function handleTextSubmit(contentId) {
  const textarea = document.getElementById(`text-${contentId}`);
  const text = textarea.value.trim();
  if (!text) { alert("내용을 입력해주세요."); return; }

  const res = await fetch(`/api/content/${contentId}/confirm`, {
    method: "PATCH",
    credentials: "include",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ confirmed: true, answer_type: "text", text }),
  });

  if (res.ok) {
    textarea.readOnly = true;
    textarea.insertAdjacentHTML("afterend",
      `<span style="margin-left:12px;color:#2e7d32;font-weight:600;">✅ 제출 완료</span>`);
    textarea.nextElementSibling?.previousElementSibling?.remove(); // 제출 버튼 제거
  } else {
    alert("제출에 실패했습니다.");
  }
}
```

### `app/routers/content.py` — confirm 페이로드 모델 수정

```python
class ConfirmPayload(BaseModel):
    confirmed: bool
    answer_type: Literal["yes_no", "text"] = "yes_no"   # response_type → answer_type
    text: str = ""   # yes_no: "Yes"|"No", text: 자유입력

@router.patch("/api/content/{content_id}/confirm")
async def confirm_content(
    content_id: str,
    payload: ConfirmPayload,
    current_user=Depends(require_authenticated_user)
):
    success = content_service.set_confirmation(
        content_id=content_id,
        emp_id=current_user["sub"],
        confirmed=payload.confirmed,
        confirmed_text=payload.text,
    )
    if not success:
        raise HTTPException(status_code=404, detail="컨텐츠를 찾을 수 없습니다.")
    return {"status": "ok"}
```

---

## 수정 요약

| PATCH | 수정 파일 | 핵심 변경 |
|---|---|---|
| A. 레이아웃 | `main.css` | 최대 너비 1240px, 2단 그리드, 모달 1100px, 최소 너비 1280px |
| B. 작성 모달 | `admin_layer.html` | 2단(좌: 대상자, 우: 입력) 와이드 레이아웃 |
| C. 모달 JS | `admin.js` | 다중선택·전체선택·검색 필터·개별 생성 Promise.all |
| D. 스키마 | `contents` 시트, `models/content.py`, `excel_repository.py` | `response_type` → `answer_type(yes_no\|text)` |
| E. 사용자 카드 | `content_layer.html`, `content.js`, `routers/content.py` | Yes/No 버튼 분기 렌더링 및 핸들러 |
